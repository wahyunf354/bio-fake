{
  "name": "bio-fake",
  "version": "1.0.0",
  "description": "library ini akan memberikan anda data yang anda perlukan untuk membuat sebuah data palsu dari seseorang",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src/*.js",
    "lint:fix": "eslint src/*.js --fix",
    "format": "prettier src/*.js --write"
  },
  "keywords": [
    "seder",
    "faker",
    "palsu",
    "bio"
  ],
  "author": "Wahyu Nur Fadillah",
  "license": "MIT",
  "dependencies": {
    "faker": "^5.1.0"
  },
  "devDependencies": {
    "eslint": "^7.10.0",
    "prettier": "^2.1.2"
  }
}
